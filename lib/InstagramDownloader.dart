import 'package:flutter/material.dart';
import 'Home.dart';

class InstagramDownloader extends StatelessWidget {

  @override
  Widget build(BuildContext context) {

    return MaterialApp(
      title: "Media Downloader for Instagram",
      home: Home()
    );
  }

}